{
  "name": "hds-group-cutlist",
  "version": "1.0.0",
  "description": "HDS Group Cutlist - Panel cutting optimization application",
  "scripts": {
    "build": "cd server && npm install && npm run build && cd ../client && npm install && npm run build",
    "start": "node server/dist/index.js",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "vercel-build": "cd server && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "private": true,
  "dependencies": {
    "@google-cloud/vision": "^5.1.0",
    "@supabase/supabase-js": "^2.50.4",
    "axios": "^1.9.0",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "form-data": "^4.0.2",
    "handlebars": "^4.7.8",
    "mongoose": "^8.0.3",
    "multer": "^2.0.0",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "@types/handlebars": "^4.1.0",
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.2"
  }
}
